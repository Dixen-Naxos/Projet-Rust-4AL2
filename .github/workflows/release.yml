on:
  push:
    branches: [ "Implement-releasing" ]

env:
  CARGO_TERM_COLOR: always
  
jobs:
  build:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Get current date
        id: date
        run: |
          echo "{date}={$(date +'%Y-%m-%d')}" >> $GITHUB_STATE
      - name: Checkout master Branch
        uses: actions/checkout@v2
        with:
          ref: 'Implement-releasing'
      - name: Build
        run: cargo build --verbose
      - name: Create Release
        id: nightly-snapshot
        uses: actions/create-release@latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: release ${{ github.date }}
          release_name: release ${{ github.date }}
          draft: false
          prerelease: false
